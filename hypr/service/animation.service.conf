animations {
    enabled = true

    bezier = myBezier, 0.05, 0.9, 0.1, 1.05
    bezier = smooth, 0.25, 0.1, 0.25, 1

    animation = windows, 1, 3, myBezier, slide
    animation = windowsOut, 1, 3, myBezier, slide
    animation = fade, 1, 3, smooth
    animation = fadeDim, 1, 3, smooth
    animation = workspaces, 1, 3, myBezier, slidevert
    animation = workspaceMonitors, 1, 3, myBezier, slide
    animation = border, 1, 4, default
    animation = borderangle, 1, 30, default, once
}
